const promptBaseStyle = "prompt-card";
const promptFaceStyle = "flex-center prompt-face";
const cardBaseStyle = "card";
const cardFaceStyle = "flex-center card-face";
const cardBackStyle = "flex-center card-back";

const GameCard = (value, baseStyle, faceStyle) => {
  let gameCard = document.createElement('div');
  gameCard.className = style;
  

  return gameCard;
}